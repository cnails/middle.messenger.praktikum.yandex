{
  "name": "yandex-chat",
  "version": "1.0.0",
  "description": "Yandex practicum project",
  "source": "src/index.html",
  "scripts": {
    "build": "parcel build src/index.html --no-source-maps --target default --dist-dir ./build",
    "watch": "parcel src/index.html --no-source-maps --target default --dist-dir ./build",
    "start": "npm run build && node server/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.4.1",
    "@parcel/transformer-less": "^2.6.2",
    "parcel": "^2.4.1"
  },
  "targets": {
    "default": {
      "sourceMap": false
    }
  },
  "dependencies": {
    "express": "^4.18.1",
    "handlebars": "^4.7.7"
  }
}
